1.什么是fast tokenizer，为什么这里必须用这个更稳定？可是文本侧一般的tokne量不是比较少吗，主要是视觉图像啊？
2.patch这里的图像size到336*336是怎么设定的，然后获取padding的尺寸是和size相关的，以及是否有其他的设定吗？比如说如果图像尺寸不一样，是否会有不同的padding设定？还有这个patch是怎么切分图像的，是固定大小的patch还是根据图像内容动态调整的？还有什么时候开启计算padding patch的索引，整成不应该是对所有有padding的图都会计算吗，为什么加了一个可选项
3.这里alpha和temp的具体是怎么设定的，我知道有消融，以及当前的P\Q都是根据相似度吧
4.后续我要融合到MM-ICL里测试一下，看看效果如何，是否需要调整一些参数或者模型结构来适应MM-ICL的需求
5.还有就是这个模型的训练数据是怎么构建的，是否有特定的策略来选择训练样本，是否有数据增强的手段来提升模型的泛化能力
6.JIT是什么东西，为什么要用它来加速模型的推理，是否有一些特定的优化策略或者技巧来利用JIT的优势，为啥不全用呢
